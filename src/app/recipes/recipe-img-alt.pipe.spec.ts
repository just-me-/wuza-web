import { RecipeImgAltPipe } from './recipe-img-alt.pipe'

describe('RecipeImgAltPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeImgAltPipe()
    expect(pipe).toBeTruthy()
  })
})
