import { RecipeImgSrcPipe } from './recipe-img-src.pipe'

describe('RecipeImgSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeImgSrcPipe()
    expect(pipe).toBeTruthy()
  })
})
